import 'package:flutter/material.dart';

class DataClanScreen extends StatelessWidget {
  const DataClanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  Center(child: Text('Soy La data del clan'),),
    );
  }
}